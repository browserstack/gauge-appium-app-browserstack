Verify Local test (LocalSample.apk)
===================================

The following scenario verifies the BrowserStack Local tunnel is connected by
launching the local sample app and asserting it reports an active connection.

Verify Local connection
-----------------------
tags: local, smoke

* I am on the local app
* I verify active connection on the app
